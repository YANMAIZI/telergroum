# GTA5RP Order Management System

## Описание

Единая система управления заявками на покупку/продажу виртов для GTA5RP, работающая офлайн через локальный backend сервер.

## Структура проекта

```
├── backend/              # Локальный Node.js сервер
│   ├── server.js         # REST API сервер
│   ├── package.json      # Зависимости
│   └── data/             # База данных (создается автоматически)
│       └── orders.json   # Файл с заявками
├── bot/                  # Telegram бот (Python)
│   └── telegram_bot_final.py
├── src/                  # Mini app (React)
│   └── App.js
└── .env                  # Конфигурация
```

## Запуск системы

### 1. Запустить Backend Server

```bash
cd backend
npm install
npm start
```

Сервер запустится на `http://localhost:3001`

### 2. Запустить Telegram Бота

```bash
cd bot
python telegram_bot_final.py
```

### 3. Запустить Mini App

```bash
npm run dev
```

## Функционал

### Telegram Бот

- ✅ Создание заявок на покупку/продажу
- ✅ Админ-панель `/admin`
- ✅ Просмотр всех заявок с датой и временем
- ✅ Одобрение/отклонение заявок на продажу
- ✅ Удаление заявок
- ✅ Редактирование цены заявок

**Админ команды:**
- `/admin` - Главное меню админа
- `/orders` - Все заявки (последние 20)
- `/orders_buy` - Заявки на покупку
- `/orders_sell` - Заявки на продажу
- `/orders_pending` - Ожидающие модерации
- `/approve_<id>` - Одобрить заявку
- `/reject_<id>` - Отклонить заявку
- `/delete_<id>` - Удалить заявку
- `/edit_<id>_<количество_кк>` - Изменить количество виртов

### Mini App

- ✅ Создание заявок на покупку/продажу
- ✅ Просмотр своих заявок
- ✅ Удаление своих заявок
- ✅ Отображение количества продавцов и виртов возле серверов
- ✅ Админ-панель для управления всеми заявками

## API Endpoints

### Orders
- `GET /api/orders` - Получить заявки (с фильтрами)
- `POST /api/orders` - Создать заявку
- `PATCH /api/orders/:id` - Обновить заявку
- `PATCH /api/orders/:id/approve` - Одобрить заявку
- `PATCH /api/orders/:id/reject` - Отклонить заявку
- `DELETE /api/orders/:id` - Удалить заявку

### Statistics
- `GET /api/orders/stats/servers` - Статистика по серверам

## Структура заявки

```json
{
  "id": "uuid",
  "order_type": "buy|sell",
  "project": "GTA5RP",
  "server_name": "DOWNTOWN",
  "server_id": 1,
  "user_id": 123456,
  "username": "user",
  "amount": 5000000,
  "price": 3450,
  "contact": "@user",
  "refund_enabled": true,
  "status": "pending|approved|rejected",
  "source": "bot|webapp",
  "created_at": "2026-02-02T14:55:00+03:00",
  "updated_at": "2026-02-02T14:55:00+03:00"
}
```

## Особенности

- 🔒 **Офлайн база данных** - Все данные хранятся локально в JSON файле
- 🔄 **Единая синхронизация** - Бот и мини-приложение работают с одной базой
- 📊 **Статистика серверов** - Автоматический подсчет продавцов и виртов
- 📅 **Дата и время** - Все заявки содержат информацию о создании/обновлении
- 👤 **Управление своими заявками** - Пользователи могут удалять свои заявки
- 👑 **Админ-панель** - Полное управление заявками через бот и мини-приложение

## Важно

- Backend сервер должен быть запущен постоянно
- Файл `backend/data/orders.json` создается автоматически
- Админ определяется по username: `patrickprodast`
